# Next Slice

1. **Apply the migrations.** `drizzle/0024_manual_payment_status.sql` and `0025_intake_content_approval.sql` are generated, additive-only, and not yet applied — run `npm run db:push` when ready. This also resolves the one `test:schema-drift` failure documented in `08-test-results.md`. Coordinate with whoever owns the concurrent Stripe Connect readiness migration also pending in `drizzle/`.
2. **Close the agent-create publish loophole.** `POST /api/agent-profiles/create` still allows an operator to pass `snaplinkStatus: "published"` directly at creation time, bypassing the intake/approval/payment gate entirely (see `07-api-enforcement.md`). This predates this task and creation flows were outside its explicit phase list — worth a small follow-up to route creation-time publish requests through the same `evaluateProfilePublicationEligibility()` check.
3. **One-time setup-fee concept.** The reused billing engine only models recurring subscription invoices (Phase 1 audit, item 4) — if a one-time setup/onboarding fee is ever needed, it does not fit the current `realEstateBillingInvoices` shape and would need its own design, not an extension of this task's status enum.
4. **Billing interval display.** `ProfessionalBillingSummary.billingInterval` was scoped but not populated — the subscription/invoice rows returned by `agentSubscriptions()`/`agentInvoices()` don't carry the plan's `billingPeriod` without an extra join, which was deprioritized in favor of the fields that were readily available (amount, currency, dates).
5. **Headless click-through verification.** No browser automation tooling is available in this environment (a recurring limitation this session) — the review panel was verified via `tsc`, `build`, and structural source assertions of its exact JSX, not by driving it in a browser.
