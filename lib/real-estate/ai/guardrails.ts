const housingRules:[string,RegExp][]=[["familial_status",/\b(?:ideal for families|no children|family-only)\b/i],["religion",/\b(?:christian neighborhood|walking distance to (?:a )?(?:church|mosque|synagogue|temple))\b/i],["marital_or_age",/\b(?:perfect for singles|young professional area|adults only)\b/i],["ethnicity",/\b(?:safe ethnic neighborhood|exclusive community for (?:specific )?groups)\b/i]];
const injectionRules=[/ignore (?:all |the )?(?:previous|prior) instructions/i,/reveal (?:the )?(?:system|hidden) prompt/i,/send (?:this |the )?data elsewhere/i,/access another (?:tenant|transaction)/i,/approve (?:this )?offer automatically/i];
export function checkFairHousing(text:string){const categories=housingRules.filter(([,r])=>r.test(text)).map(([k])=>k);return{safe:categories.length===0,categories,explanation:categories.length?"Content may imply preference or limitation based on a protected class. Use objective property facts only.":undefined,compliantRewrite:categories.length?"Describe verified property features, condition, amenities, and location without describing preferred occupants.":undefined}}
export function detectPromptInjection(text:string){const matches=injectionRules.filter(r=>r.test(text)).length;return{safe:matches===0,category:matches?"prompt_injection":undefined}}
export function assertSafeGeneratedContent(feature:string,text:string){if(["property_description","market_insights"].includes(feature)){const result=checkFairHousing(text);if(!result.safe)throw Object.assign(new Error(result.explanation),{code:"fair_housing_guardrail",permanent:true})}return true}
