import { createCipheriv, createDecipheriv, createHash, randomBytes, timingSafeEqual } from "node:crypto";
const keyBytes = (key) => { if (!key || key.length < 32) throw new Error("REAL_ESTATE_TOKEN_ENCRYPTION_KEY must be at least 32 characters"); return createHash("sha256").update(key).digest(); };
export function encryptSecretValue(plaintext, key) { const iv=randomBytes(12),cipher=createCipheriv("aes-256-gcm",keyBytes(key),iv),ciphertext=Buffer.concat([cipher.update(plaintext,"utf8"),cipher.final()]),tag=cipher.getAuthTag();return`v1.${iv.toString("base64url")}.${tag.toString("base64url")}.${ciphertext.toString("base64url")}`; }
export function decryptSecretValue(envelope,key){const[version,iv,tag,ciphertext]=envelope.split(".");if(version!=="v1"||!iv||!tag||!ciphertext)throw new Error("Invalid encrypted token envelope");const decipher=createDecipheriv("aes-256-gcm",keyBytes(key),Buffer.from(iv,"base64url"));decipher.setAuthTag(Buffer.from(tag,"base64url"));return Buffer.concat([decipher.update(Buffer.from(ciphertext,"base64url")),decipher.final()]).toString("utf8");}
export function signOpaqueValue(value,key){return`${value}.${createHash("sha256").update(`${key}:${value}`).digest("base64url")}`}
export function verifyOpaqueValue(signed,key){const split=signed.lastIndexOf(".");if(split<1)return null;const value=signed.slice(0,split),expected=signOpaqueValue(value,key).slice(split+1),actual=signed.slice(split+1);return expected.length===actual.length&&timingSafeEqual(Buffer.from(expected),Buffer.from(actual))?value:null}
export function safeHashValue(value){return createHash("sha256").update(value.trim().toLowerCase()).digest("hex")}
