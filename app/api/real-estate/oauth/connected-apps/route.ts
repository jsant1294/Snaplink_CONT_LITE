import{NextRequest,NextResponse}from"next/server";import{authorizeRealEstate}from"@/lib/real-estate/auth";import{listConnectedApps,revokeConsent}from"@/lib/real-estate/enterprise/oauth";
export async function GET(req:NextRequest){const p=await authorizeRealEstate(req,"settings:manage");if(!p)return NextResponse.json({error:"Access denied"},{status:403});return NextResponse.json({apps:await listConnectedApps({tenantId:p.tenantId},p.membershipId)})}
export async function DELETE(req:NextRequest){const p=await authorizeRealEstate(req,"settings:manage");if(!p)return NextResponse.json({error:"Access denied"},{status:403});const body=await req.json().catch(()=>({}));const clientId=String((body as{clientId?:string}).clientId||req.nextUrl.searchParams.get("clientId")||"");if(!clientId)return NextResponse.json({error:"clientId is required"},{status:400});return NextResponse.json(await revokeConsent({tenantId:p.tenantId},p.membershipId,clientId))}
