import { NextRequest, NextResponse } from "next/server"; import { authorizeRealEstate } from "@/lib/real-estate/auth"; import { sendProfessionalMessage } from "@/lib/real-estate/transactions/collaboration";
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { const principal = await authorizeRealEstate(req, "transactions:manage"); if (!principal) return NextResponse.json({ error: "Access denied" }, { status: 403 }); try { return NextResponse.json({ record: await sendProfessionalMessage(principal, principal.membershipId, (await params).id, String((await req.json()).body || "")) }, { status: 201 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to send message" }, { status: 400 }); } }
